import { Layout } from "src/shared/ui/layout/ui/Layout";
import { EmployeesList } from "src/widgets/employees-list";

export const AdminEmployeesPage = () => {
  return (
    <Layout>
      <EmployeesList />
    </Layout>
  );
};
