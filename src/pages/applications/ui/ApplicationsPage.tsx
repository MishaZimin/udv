import { Layout } from "src/shared/ui/layout/ui/Layout";
import { AllApplicationsList } from "./ApplicationsList";

export const AdminApplicationPage = () => {
  return (
    <Layout>
      <AllApplicationsList />
    </Layout>
  );
};
